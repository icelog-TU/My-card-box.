# 培嘉卡片盒 — 使用說明

---

## 一、APP 網址

```
https://icelog-tu.github.io/My-card-box./
```

在任何裝置的瀏覽器輸入這個網址就能打開 App。
建議加入書籤（加到主畫面捷徑）方便日後開啟。

---

## 二、雲端同步怎麼做

### 同步金鑰是什麼？
每台裝置第一次開啟 App 時，會自動產生一串隨機金鑰（例如：`a3b2c4-x9k1m2-p7q8r3`）。
**金鑰就是你在雲端的「抽屜號碼」**，有這個號碼的裝置才能存取你的資料。

### 如何取得金鑰？
1. 打開 App
2. 點右上角 **「同步」** 按鈕
3. 「同步金鑰」欄位裡就是你的金鑰
4. 按 **「複製」** 可複製到剪貼簿

> 建議把金鑰抄下來或儲存在備忘錄，以防換手機或清除瀏覽器資料時遺失。

### 上傳資料到雲端（備份 / 換裝置前）
1. 點「同步」
2. 點 **「上傳到雲端」**
3. 出現「上傳成功」即完成

### 從雲端載入資料（換裝置 / 想拿最新資料）
1. 點「同步」
2. 點 **「從雲端載入」**
3. 確認覆蓋後即完成

### 第一次連結新裝置（手機、平板等）
1. 在舊裝置複製金鑰
2. 在新裝置打開 App → 點「同步」
3. 將金鑰貼入「使用其他裝置的金鑰」欄位 → 按 **「套用」**
4. 按 **「從雲端載入」**

> 之後兩台裝置共用同一個金鑰，不需要重複設定。

### 多裝置使用建議
- 每次開始工作前：先「從雲端載入」
- 每次工作結束後：「上傳到雲端」
- 避免同時在兩台裝置編輯，後上傳的會覆蓋先上傳的

---

## 三、Firestore 雲端管理頁面

你的資料存放在 **Firebase Firestore** 資料庫。

### Firebase 後台（可查看、管理資料）
```
https://console.firebase.google.com/project/pei-chia-card-box/firestore
```
登入帳號：icelog@gmail.com

資料存放位置：
- 集合（Collection）：`cardboxes`
- 文件（Document）：你的同步金鑰字串
- 欄位（Field）：`data`（JSON 格式）、`updatedAt`（時間戳記）

> ⚠️ 注意：目前 Firestore 設定為「測試模式」，30 天後可能到期需更新安全規則。
> 如果同步突然失敗，請到 Firebase 後台 → Firestore → 規則，延長測試期或設定正式規則。

---

## 四、本機備份 — 匯出與匯入 JSON

App 內建匯出／匯入功能，入口在「同步」視窗的最下方「**本機備份**」區塊。

### 匯出備份（把資料存到 Google 雲端硬碟）
1. 點右上角 **「同步」** 按鈕
2. 滑到視窗底部，點 **「匯出 JSON」**
3. 瀏覽器會自動下載一個檔案，例如：`培嘉卡片盒備份_2026-06-15.json`
4. 打開 Google 雲端硬碟，把這個 `.json` 檔案拖進去存放即可

> 建議定期匯出一次（例如每個月），存到 Google 雲端硬碟當作長期備份。

### 匯入備份（從備份檔案還原資料）
1. 點右上角 **「同步」** 按鈕
2. 滑到視窗底部，點 **「匯入 JSON」**
3. 選擇之前匯出的 `.json` 備份檔案
4. 確認覆蓋後，所有資料立即還原

> ⚠️ 匯入會**完整覆蓋**目前裝置上的資料，若想保留現有資料請先匯出一份備份。

### 備份檔案放哪裡？
建議在 Google 雲端硬碟建立一個資料夾，例如：
```
我的雲端硬碟 / 培嘉卡片盒備份 / 培嘉卡片盒備份_2026-06-15.json
```
舊版本不用刪，保留多個日期的備份更安全。

### 備用方式：從 Firebase 後台取出資料
如果 App 無法開啟，也可以直接從雲端資料庫取出：
1. 開啟 Firebase 後台（見第三節網址）
2. 點 `cardboxes` 集合 → 點你的同步金鑰文件
3. 找到 `data` 欄位，複製裡面的 JSON 內容
4. 貼到記事本，儲存為 `.json` 檔案

---

## 五、原始程式碼位置

| 項目 | 位置 |
|------|------|
| 本機原始檔 | `D:\AI vibe coding\培嘉卡片盒\index.html` |
| GitHub 儲存庫 | `https://github.com/icelog-tu/My-card-box.` |
| GitHub Pages 網址 | `https://icelog-tu.github.io/My-card-box./` |

更新 App 的方式：修改 `index.html` → 上傳到 GitHub → 幾分鐘後網頁自動更新。

---

*最後更新：2026-06-15*
